import EStyleSheet from 'react-native-extended-stylesheet';

const DescriptionStyles = EStyleSheet.create({
    container: {
        width: '100%'
    },
    imageWrappper: {
        marginLeft: 'auto',
        marginRight: 'auto'
    }
  });

    export default DescriptionStyles;